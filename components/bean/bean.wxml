<block wx:for="{{beans}}" wx:key="{{index}}">
  <view class="bean-wrap" style="animation-name:wrap-{{item.ratio}}; left:{{item.left}}; top:{{item.top}}">
    <view class="bean"></view>
  </view>
</block>